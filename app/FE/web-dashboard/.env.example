# =============================================================================
# SmartDine — Web Dashboard (Vercel)
# Trong Vercel Project → Settings → Environment Variables
# Vite nhúng biến lúc BUILD — đổi env phải Redeploy.
# =============================================================================

# URL Gateway Render (có /api/v1, KHÔNG có slash cuối)
VITE_API_URL=https://YOUR_GATEWAY.onrender.com/api/v1
